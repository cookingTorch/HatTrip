<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.mapper.CommentMapper">

    <resultMap id="commentMap" type="com.ssafy.dto.CommentDto">
        <result property="commentNo" column="comment_no" />
        <result property="boardNo" column="board_no" />
        <result property="userId" column="user_id" />
        <result property="content" column="content" />
        <result property="createAt" column="create_at" />
    </resultMap>

    <!-- 댓글 추가 -->
    <insert id="addComment" parameterType="com.ssafy.dto.CommentDto">
        INSERT INTO comment (board_no, user_id, content, create_at)
        VALUES (#{boardNo}, #{userId}, #{content}, NOW())
    </insert>

    <!-- 특정 게시글의 댓글 목록 조회 -->
    <select id="listComments" resultMap="commentMap">
        SELECT comment_no, board_no, user_id, content, create_at
        FROM comment
        WHERE board_no = #{boardNo}
        ORDER BY create_at ASC
    </select>

    <!-- 댓글 삭제 -->
    <delete id="deleteComment">
        DELETE FROM comment
        WHERE comment_no = #{commentNo}
    </delete>

    <!-- 댓글 수정 -->
    <update id="updateComment" parameterType="com.ssafy.dto.CommentDto">
        UPDATE comment
        SET content = #{content},
            create_at = NOW()
        WHERE comment_no = #{commentNo}
    </update>

</mapper>
